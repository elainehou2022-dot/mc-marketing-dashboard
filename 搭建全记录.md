# MC 营销看板 — 搭建全记录

> 创建时间：2026-06-05  ·  最后更新：2026-06-09
> 负责人：Elaine  ·  托管：GitHub Pages

---

## 1. 项目背景

**任务**：为 MC（虚拟货币交易平台，官网 [mcmarkets.com](https://www.mcmarkets.com/zh-Hant/trade/BTCUSDC)）搭建「营销部门看板」网页版，整合进公司总台营销分页。

**技术要求**：
- 网页形式，有公开可访问的 URL
- 优先纯静态，允许 iframe 嵌入（不设 X-Frame-Options: DENY）
- 用示例数据/占位即可

**三大模块**：
- 营销物料库（物料名 / 类型 / 语言 / 链接 / 状态）
- 渠道 / 活动看板（活动名 / 渠道 / 预算 / 状态 / 负责人）
- 营销 SOP（物料制作流程 / 多语言本地化流程 / KOL 合作流程）

---

## 2. 技术方案

| 项目 | 选型 |
|------|------|
| 页面形式 | 单文件 HTML（CSS / JS 全部内嵌，零外部依赖） |
| 图片素材 | Unsplash 公开图片（12 张，按主题匹配），加载失败自动降级 |
| 托管 | GitHub Pages（`elainehou2022-dot/mc-marketing-dashboard`，`main` 分支根目录） |
| 本地源码 | `/Users/elaine/mc-marketing-dashboard/index.html` |

---

## 3. 部署历程（三次迭代）

### 3.1 第一次：localtunnel（失败）

```bash
NPM_CONFIG_CACHE=/tmp/npm-cache npx -y localtunnel --port 8765
# URL: https://fifty-seas-buy.loca.lt
```

**问题**：localtunnel 会在首次访问时弹出「Tunnel website ahead!」水印页，体验极差，放弃。

### 3.2 第二次：Surge.sh（被拒）

```bash
npx surge --domain mc-marketing-xxx.surge.sh ./
```

**问题**：Surge 检测到临时邮箱注册，返回 HTTP 451（不可用）。

### 3.3 第三次：Cloudflare Quick Tunnel（临时用）

```bash
python3 -m http.server 8765 &
./cloudflared tunnel --url http://localhost:8765 --no-autoupdate
# URL: https://suggests-identifies-scripting-delayed.trycloudflare.com
```

**可用但不持久**：本地 Python 进程和 cloudflared 必须一直跑，电脑关机 / Claude 退出后 URL 立即失效。

### 3.4 第四次：GitHub Pages（✅ 最终方案）

```bash
# 初始化仓库
cd /Users/elaine/mc-marketing-dashboard
git init
git add .nojekyll README.md index.html
git commit -m "MC 营销看板 v1.0"

# 安装 GitHub CLI
curl -sL "https://github.com/cli/cli/releases/download/v2.93.0/gh_2.93.0_macOS_arm64.zip" -o /tmp/gh.zip
unzip /tmp/gh.zip -d /tmp/gh_extract
cp /tmp/gh_extract/gh_*/bin/gh /Users/elaine/mc-marketing-dashboard/gh

# 授权（浏览器设备码流程）
./gh auth login --hostname github.com --git-protocol https --web
# → 打开 https://github.com/login/device → 输入一次性验证码 → 授权

# 推送
git remote add origin https://github.com/elainehou2022-dot/mc-marketing-dashboard.git
git push origin main --force

# GitHub Pages 自动部署
# URL: https://elainehou2022-dot.github.io/mc-marketing-dashboard/
```

---

## 4. 修复记录

### 4.1 物料卡片点击无效 → 事件委托

**根因**：`onclick="openMaterial(N)"` 写在卡片 div 上，子元素（badge、type-badge、view-hint）拦截冒泡事件。

**修复**：
```js
// 改为事件委托，用 closest('.mat') 查找祖先
matGrid.addEventListener("click", e => {
  const card = e.target.closest(".mat");
  if (!card) return;
  const id = parseInt(card.dataset.id, 10);
  openMaterial(id);
});
```

### 4.2 占位缩略图 → 真实营销图

将 12 个物料的渐变占位替换为 Unsplash 真实图片，按主题匹配：

| # | 物料 | 图片主题 |
|---|------|----------|
| 1 | BTC 现货交易主视觉 | 比特币特写 |
| 2 | 新用户注册奖励落地页 | 礼包/欢迎礼 |
| 3 | 合约交易 100x 杠杆文案 | 加密交易终端 |
| 4 | YouTube Shorts 60s 教学 | 摄像机/视频创作 |
| 5 | 夏季交易节 Banner | 夏日/度假风 |
| 6 | VIP 等级权益 EDM | 黑金 VIP 质感 |
| 7 | KOL 招募计划 H5 | 主播/麦克风 |
| 8 | ETH 合并周年纪念海报 | 以太坊视觉 |
| 9 | Web3 钱包教学文案 | 区块链/数字钱包 |
| 10 | 机构客户白皮书 | 现代办公场景 |
| 11 | 圣诞节 NFT 活动 | 圣诞节日氛围 |
| 12 | App 下载引导 | 手机应用界面 |

CSS 增强：
```css
.mat { cursor: pointer; transition: transform .2s, box-shadow .2s; }
.mat:hover { transform: translateY(-3px); box-shadow: 0 12px 32px rgba(0,0,0,.45); }
.mat:hover .thumb img { transform: scale(1.06); }
.mat .thumb.no-img { /* 图片加载失败降级为渐变+图标 */ }
```

### 4.3 品牌名：MC Markets → MC

全文搜索替换，3 处全部改完（`<title>`、顶栏、页脚），零残留。

### 4.4 「打开素材」按钮无效 → 真正可跳转

```js
// 模态框内按钮用 data-open-link 属性 + addEventListener 绑定
openBtn.addEventListener('click', () => {
  const link = openBtn.getAttribute('data-open-link');
  if (link && link !== '#') window.open(link, '_blank', 'noopener,noreferrer');
  else alert('该物料暂无外部链接（演示数据）');
});
```

### 4.5 仓库瘦身

添加 `.gitignore`，移除误提交的 38MB `gh` 二进制文件。

---

## 5. 最终文件结构

```
/Users/elaine/mc-marketing-dashboard/
├── index.html       # 看板主文件（单文件，CSS/JS 内嵌）
├── README.md         # 项目说明
├── .nojekyll         # 告诉 GitHub Pages 跳过 Jekyll 处理
├── .gitignore        # 忽略 gh / node_modules / .DS_Store
└── .git/             # Git 仓库（已关联远程 origin）
```

---

## 6. 日常维护

### 修改看板内容

```bash
cd /Users/elaine/mc-marketing-dashboard

# 1. 编辑 index.html（直接改 MATERIALS / CAMPAIGNS / CHANNELS 数组）
# 2. 提交并推送
git add index.html
git commit -m "更新: 描述你改了什么"
git push origin main

# 3. 等 1-2 分钟，GitHub Pages 自动部署
# 4. 访问 https://elainehou2022-dot.github.io/mc-marketing-dashboard/ 验证
```

### 数据源头（当前为示例/占位）

| 数据 | JavaScript 常量 | 位置（行号参考） |
|------|-----------------|-------------------|
| 物料列表 | `MATERIALS` | `index.html` 底部 `<script>` 块 |
| 活动列表 | `CAMPAIGNS` | 同上 |
| 渠道数据 | `CHANNELS` | 同上 |

生产环境对接时需要替换为真实 API 或嵌入后端数据。

### 如果需要重新拉代码

```bash
git clone https://github.com/elainehou2022-dot/mc-marketing-dashboard.git
cd mc-marketing-dashboard
# 编辑 index.html，然后 git push
```

---

## 7. 网址速查

| 用途 | 地址 |
|------|------|
| **生产 URL**（永久） | https://elainehou2022-dot.github.io/mc-marketing-dashboard/ |
| GitHub 仓库 | https://github.com/elainehou2022-dot/mc-marketing-dashboard |
| MC 官网（参考） | https://www.mcmarkets.com/zh-Hant/trade/BTCUSDC |
| ~~临时 URL~~（已废弃） | https://suggests-identifies-scripting-delayed.trycloudflare.com |

---

## 8. 关键验证项

在部署后确认以下各项均通过：

- [x] HTTP 200，页面正常加载
- [x] 页面标题 `MC · 营销看板 (Marketing Dashboard)`
- [x] 12 张 Unsplash 营销图全部就位
- [x] 点击物料卡片 → 弹出详情模态框 → 「打开素材 ↗」可跳转
- [x] 事件委托工作正常（不受子元素拦截）
- [x] `MC Markets` 残留为 0（已全部改为 `MC`）
- [x] iframe 友好：无 `X-Frame-Options` / 无 `frame-ancestors` 限制
- [x] `access-control-allow-origin: *`
- [x] GitHub Pages 状态 `built`

---

## 9. 合规红线（已写入看板 SOP 区）

- **禁止** 任何「保本 / 稳赚 / 翻倍」收益承诺
- **禁止** 展示具体盈利用户案例（仅可展示策略/教学）
- 必含 **风险提示语**（「加密资产价格波动剧烈，可能损失全部本金」）
- 受邀 KOL 必须在显著位置披露 **赞助关系**（#ad / #赞助）
- 不同地区需附对应监管标识（香港 SFC、日本 FSA、欧洲 MiCA 等）

---

## 10. 团队示例（占位）

| 角色 | 姓名 |
|------|------|
| Google Ads / 联盟营销 | Cherry L. |
| Meta Ads / TikTok | Marcus W. |
| KOL 合作 / YouTube | Iris T. |
| EDM / X (Twitter) | Eric H. |
| 线下活动 / 越南市场 | Vivian Z. |

---

*最后更新：2026-06-09 · 使用模型：MiniMax-M3*
